# 知识星球 P={S0,Sg,O,C} 方法论执行提示词 07

## 任务定义

请基于 `P = { S0, Sg, O, C }` 的方式推进当前仓库中的知识星球模块开发，并直接在仓库内落地代码。

## 现状 S0

- 仓库已经有一部分实现
- 管理端前端在 `admin-web`
- 后端在 `backend`
- 小程序端在 `miniprogram`
- 当前后端管理相关功能和部分页面还原度不够
- 至少有一个页面明显“不像知识星球”
- 目前很多功能需要结合数据库和测试数据才能验证

## 目标 Sg

在遵守现有技术约束的前提下，形成一个可运行、可验证、结构清晰、便于继续扩展的知识星球模块，同时覆盖：

1. 用户视角
2. 管理视角
3. 后端接口与数据模型
4. 测试数据与验证闭环

## 操作子 O

你可以使用的关键动作包括：

- 阅读仓库代码
- 分析真实网页 DOM 和 Network
- 对照 `admin-web`、`backend`、`miniprogram`
- 读取数据库
- 设计测试数据
- 修正接口、页面、状态流转、数据模型
- 完成功能后回归验证

## 约束 C

- 小程序端必须严格遵守 `AGENTS.md`
- 小程序只能使用原生微信小程序能力
- 参考真实站点：`https://wx.zsxq.com/group/28882128518851`
- 管理视角从右上角“管理后台”进入
- 优先相信真实 DOM 和真实 Network，而不是凭感觉猜
- 每个功能都要先设计测试数据
- 功能完成后必须用测试数据验证
- 第一轮以“先有可运行版本”为目标，不追求一次性完美

## 问题拉力识别

请先明确回答：为什么当前仓库无法自然演进到目标状态？核心阻碍可能来自哪些方面？

至少从这几个角度判断：

- 信息缺失：真实字段和接口还原不够
- 结构偏差：管理端和后端的数据关系不对
- 验证不足：没有成体系测试数据
- 页面偏差：最不像的页面没有被优先修复

## 执行要求

请按以下步骤推进：

1. 盘点当前 `admin-web`、`backend`、`miniprogram` 的已完成功能
2. 找出距离 `Sg` 最远的页面和模块
3. 为每个优先模块先设计测试数据
4. 开始落地修复
5. 每次修复后都做数据库、接口、页面联动验证
6. 输出剩余问题拉力和下一轮操作建议

## 验证标准

每一项完成后至少验证：

- 数据库是否正确
- 接口返回是否正确
- 管理端是否正确
- 用户端是否正确
- 与真实知识星球相比还差什么

## 输出要求

不要只写推理。你需要给出：

1. 代码修改
2. 测试数据方案
3. 验证结论
4. 未解决项

请立刻开始，不要停在抽象层面。
