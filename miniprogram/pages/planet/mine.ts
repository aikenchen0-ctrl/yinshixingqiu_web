import { registerMinePage } from '../../utils/mine-page'

registerMinePage({
  showTipCard: true,
  tipTitle: '为什么这里还保留“我的”',
  tipDesc: '饮视星球是子模块，登录入口放在这里，避免你被带去外层页面来回跳。',
  showPlanetBottomNav: true,
  menuMode: 'planet',
})
